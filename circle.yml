general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone:
    Asia/Calcutta
  node:
    version: 6.9.1
dependencies:
  override: 
    - npm install -g harp
  cache_directories:
    - /opt/circleci/nodejs/v6.9.1/lib/node_modules
    - /opt/circleci/nodejs/v6.9.1/bin
#checkout:
#  post:
#    - cd www
#    - git checkout gh-pages
#    - cd ..
    
#    - ./scripts/build.sh

test:
  override:
    - exit 0

deployment:
  production:
    branch: master
    commands:
      - sh build.sh
